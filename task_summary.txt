# Fix for Issue #2228: Add schedule trigger to nightly build

## Changes
- Modified `.github/workflows/Build_mR-NB.yml`:
  - Added `schedule` trigger for 03:00 UTC daily.
  - Updated `build` job `if` condition to include `github.event_name == 'schedule'`.

## Verification
- Ran `build.ps1`: **Success**
- Ran `dotnet test`: **Partial Success** (444 tests passed, then host crash - unrelated to YAML change).
